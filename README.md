# ExcelFunctionsTranslationsRuEng
Обновляемая таблица соответствия функций Excel на русском и английском языке. Список основан на официальной справке. 

|  Function | Description | Функция | Описание |
|  ------ | ------ | ------ | ------ |
|  SUBTOTAL | Returns a subtotal in a list or database | ПРОМЕЖУТОЧНЫЕ.ИТОГИ | Возвращает промежуточный итог в списке или базе данных. |
|  SUM | Adds its arguments | СУММ | Суммирует аргументы. |
|  SUMIF | Adds the cells specified by a given criteria | СУММЕСЛИ | Суммирует ячейки, удовлетворяющие заданному условию. |
|  SQRTPI | Returns the square root of (number * pi) | КОРЕНЬПИ | Возвращает квадратный корень из значения выражения (число * пи). |
|  SIN | Returns the sine of the given angle | SIN | Возвращает синус заданного угла. |
|  SINH | Returns the hyperbolic sine of a number | SINH | Возвращает гиперболический синус числа. |
|  SQRT | Returns a positive square root | КОРЕНЬ | Возвращает положительное значение квадратного корня. |
|  SUMIFS | Adds the cells in a range that meet multiple criteria | СУММЕСЛИМН | Суммирует ячейки в диапазоне, удовлетворяющие нескольким условиям. |
|  SUMXMY2 | Returns the sum of squares of differences of corresponding values in two arrays | СУММКВРАЗН | Возвращает сумму квадратов разностей соответствующих значений в двух массивах. |
|  TAN | Returns the tangent of a number | TAN | Возвращает тангенс числа. |
|  TANH | Returns the hyperbolic tangent of a number | TANH | Возвращает гиперболический тангенс числа. |
|  SUMX2PY2 | Returns the sum of the sum of squares of corresponding values in two arrays | СУММСУММКВ | Возвращает сумму сумм квадратов соответствующих элементов двух массивов. |
|  SUMPRODUCT | Returns the sum of the products of corresponding array components | СУММПРОИЗВ | Возвращает сумму произведений соответствующих элементов массивов. |
|  SUMSQ | Returns the sum of the squares of the arguments | СУММКВ | Возвращает сумму квадратов аргументов. |
|  SUMX2MY2 | Returns the sum of the difference of squares of corresponding values in two arrays | СУММРАЗНКВ | Возвращает сумму разностей квадратов соответствующих значений в двух массивах. |
|  RADIANS | Converts degrees to radians | РАДИАНЫ | Преобразует градусы в радианы. |
|  RAND | Returns a random number between 0 and 1 | СЛЧИС | Возвращает случайное число в интервале от 0 до 1. |
|  RANDBETWEEN | Returns a random number between the numbers you specify | СЛУЧМЕЖДУ | Возвращает случайное число в интервале между двумя заданными числами. |
|  QUOTIENT | Returns the integer portion of a division | ЧАСТНОЕ | Возвращает целую часть частного при делении. |
|  PI | Returns the value<br/>of pi | ПИ | Возвращает число пи. |
|  POWER | Returns the result of a number raised to a power | СТЕПЕНЬ | Возвращает результат возведения числа в степень. |
|  PRODUCT | Multiplies its arguments | ПРОИЗВЕД | Возвращает произведение аргументов. |
|  ROMAN | Converts an Arabic numeral to Roman, as text | РИМСКОЕ | Преобразует арабские числа в римские в виде текста. |
|  SECH | Returns the hyperbolic secant of an angle | SECH | Возвращает гиперболический секанс угла. |
|  SERIESSUM | Returns the sum of a power series based on the formula | РЯД.СУММ | Возвращает сумму степенного ряда, вычисленную по формуле. |
|  SIGN | Returns the sign of a number | ЗНАК | Возвращает знак числа. |
|  SEC | Returns the secant of an angle | SEC | Возвращает секанс угла. |
|  ROUND | Rounds a number to a specified number of digits | ОКРУГЛ | Округляет число до указанного количества десятичных разрядов. |
|  ROUNDDOWN | Rounds a number down, toward zero | ОКРУГЛВНИЗ | Округляет число до ближайшего меньшего по модулю значения. |
|  ROUNDUP | Rounds a number up, away from zero | ОКРУГЛВВЕРХ | Округляет число до ближайшего большего по модулю значения. |
|  COUNT | Counts how many numbers are in<br/>the list of arguments | СЧЁТ | Подсчитывает количество чисел в списке аргументов. |
|  COUNTA | Counts how many values are in the list of arguments | СЧЁТЗ | Подсчитывает количество значений в списке аргументов. |
|  COUNTBLANK | Counts the number of blank cells within a range | СЧИТАТЬПУСТОТЫ | Подсчитывает количество пустых ячеек в диапазоне. |
|  CORREL | Returns the correlation coefficient between two data sets | КОРРЕЛ | Возвращает коэффициент корреляции между двумя множествами данных. |
|  CHISQ.TEST | Returns the test for independence | ХИ2.ТЕСТ | Возвращает тест на независимость. |
|  CONFIDENCE.NORM | Returns the confidence interval for a population mean | ДОВЕРИТ.НОРМ | Возвращает доверительный интервал для среднего значения по генеральной совокупности. |
|  CONFIDENCE.T | Returns the confidence interval for a population mean, using a Student's t distribution | ДОВЕРИТ.СТЬЮДЕНТ | Возвращает доверительный интервал для среднего генеральной совокупности, используя t-распределение Стьюдента. |
|  COUNTIF | Counts the number of cells within a range that meet the given criteria | СЧЁТЕСЛИ | Подсчитывает количество ячеек в диапазоне, удовлетворяющих заданному условию. |
|  EXPON.DIST | Returns the exponential distribution | ЭКСП.РАСП | Возвращает экспоненциальное распределение. |
|  F.DIST | Returns the F probability distribution | F.РАСП | Возвращает F-распределение вероятности. |
|  F.DIST.RT | Returns the F probability distribution | F.РАСП.ПХ | Возвращает F-распределение вероятности. |
|  DEVSQ | Returns the sum of squares of deviations | КВАДРОТКЛ | Возвращает сумму квадратов отклонений. |
|  COUNTIFS | Counts the number of cells within a range that meet multiple criteria | СЧЁТЕСЛИМН | Подсчитывает количество ячеек внутри диапазона, удовлетворяющих нескольким условиям. |
|  COVARIANCE.P | Returns covariance, the average of the products of paired deviations | КОВАРИАЦИЯ.Г | Возвращает ковариацию, среднее произведений парных отклонений. |
|  COVARIANCE.S | Returns the sample covariance, the average of the products deviations for each data point pair in two data sets | КОВАРИАЦИЯ.В | Возвращает ковариацию выборки — среднее попарных произведений отклонений для всех точек данных в двух наборах данных. |
|  AVERAGEIF | Returns the average (arithmetic mean) of all the cells in a range that meet a given criteria | СРЗНАЧЕСЛИ | Возвращает среднее значение (среднее арифметическое) всех ячеек в диапазоне, которые удовлетворяют данному условию. |
|  AVERAGEIFS | Returns the average (arithmetic mean) of all cells that meet multiple criteria | СРЗНАЧЕСЛИМН | Возвращает среднее значение (среднее арифметическое) всех ячеек, которые удовлетворяют нескольким условиям. |
|  BETA.DIST | Returns the beta cumulative distribution function | БЕТА.РАСП | Возвращает интегральную функцию бета-распределения. |
|  AVERAGEA | Returns the average of its arguments, including numbers, text, and logical values | СРЗНАЧА | Возвращает среднее арифметическое аргументов, включая числа, текст и логические значения. |
|  TRUNC | Truncates a number to an integer | ОТБР | Отбрасывает дробную часть числа. |
|  AVEDEV | Returns the average of the absolute deviations of data points from their mean | СРОТКЛ | Возвращает среднее арифметическое абсолютных значений отклонений точек данных от среднего. |
|  AVERAGE | Returns the average of its arguments | СРЗНАЧ | Возвращает среднее арифметическое аргументов. |
|  BETA.INV | Returns the inverse of the cumulative distribution function for a specified beta distribution | БЕТА.ОБР | Возвращает обратную интегральную функцию указанного бета-распределения. |
|  CHISQ.DIST.RT | Returns the one-tailed probability of the chi-squared distribution | ХИ2.РАСП.ПХ | Возвращает одностороннюю вероятность распределения хи-квадрат. |
|  CHISQ.INV | Returns the cumulative beta probability density function | ХИ2.ОБР | Возвращает интегральную функцию плотности бета-вероятности. |
|  CHISQ.INV.RT | Returns the inverse of the one-tailed probability of the chi-squared distribution | ХИ2.ОБР.ПХ | Возвращает обратное значение односторонней вероятности распределения хи-квадрат. |
|  CHISQ.DIST | Returns the cumulative beta probability density function | ХИ2.РАСП | Возвращает интегральную функцию плотности бета-вероятности. |
|  BINOM.DIST | Returns the individual term binomial distribution probability | БИНОМ.РАСП | Возвращает отдельное значение вероятности биномиального распределения. |
|  BINOM.DIST.RANGE | Returns the probability of a trial result using a binomial distribution | БИНОМ.РАСП.ДИАП | Возвращает вероятность пробного результата с помощью биномиального распределения. |
|  BINOM.INV | Returns the smallest value for which the cumulative binomial distribution is less than or equal to a criterion value | БИНОМ.ОБР | Возвращает наименьшее значение, для которого интегральное биномиальное распределение меньше заданного значения или равно ему. |
|  ASIN | Returns the arcsine of a number | ASIN | Возвращает арксинус числа. |
|  ASINH | Returns the inverse hyperbolic sine of a number | ASINH | Возвращает гиперболический арксинус числа. |
|  ATAN | Returns the arctangent of a number | ATAN | Возвращает арктангенс числа. |
|  ARABIC | Converts a Roman number to Arabic, as a number | АРАБСКОЕ | Преобразует римские числа в арабские в виде числа. |
|  ACOT | Returns the arccotangent of a number | ACOT | Возвращает арккотангенс числа. |
|  ACOTH | Returns the hyperbolic arccotangent of a number | ACOTH | Возвращает гиперболический арккотангенс числа. |
|  AGGREGATE | Returns an aggregate in a list or database | АГРЕГАТ | Возвращает агрегированное выражение списка или базы данных. |
|  ATAN2 | Returns the arctangent from x- and y-coordinates | ATAN2 | Возвращает арктангенс для заданных координат x и y. |
|  CEILING.PRECISE | Rounds a number the nearest integer or to the nearest multiple of significance. Regardless of the sign of the number, the number is rounded up. | ОКРВВЕРХ.ТОЧН | Округляет число в большую сторону (независимо от его знака) до ближайшего целого или кратного. |
|  COMBIN | Returns the number of combinations for a given number of objects | ЧИСЛКОМБ | Возвращает количество комбинаций для заданного числа объектов. |
|  COMBINA | Returns the number of combinations with repetitions for a given number of items | ЧИСЛКОМБА | Возвращает количество комбинаций с повторами для заданного числа элементов. |
|  CEILING.MATH | Rounds a number up, to the nearest integer or to the nearest multiple of significance | ОКРВВЕРХ.МАТ | Округляет число в большую сторону до ближайшего целого или кратного. |
|  ATANH | Returns the inverse hyperbolic tangent of a number | ATANH | Возвращает гиперболический арктангенс числа. |
|  BASE | Converts a number into a text representation with the given radix (base) | ОСНОВАНИЕ | Преобразует число в текстовое представление с данным основанием (базой). |
|  CEILING | Rounds a number to the nearest integer or to the nearest multiple of significance | ОКРВВЕРХ | Округляет число до ближайшего целого или кратного. |
|  LOOKUP | Looks up values in a vector or array | ПРОСМОТР | Ищет значения в векторе или массиве. |
|  MATCH | Looks up values in a reference or array | ПОИСКПОЗ | Ищет значения в ссылке или массиве. |
|  OFFSET | Returns a reference offset from a given reference | СМЕЩ | Возвращает смещение ссылки относительно заданной ссылки. |
|  INDIRECT | Returns a reference indicated by a text value | ДВССЫЛ | Возвращает ссылку, заданную текстовым значением. |
|  HLOOKUP | Looks in the top row of an array and returns the value of the indicated cell | ГПР | Выполняет поиск в первой строке массива и возвращает значение указанной ячейки. |
|  HYPERLINK | Creates a shortcut or jump that opens a document stored on a network server, an intranet, or the Internet | ГИПЕРССЫЛКА | Создает ссылку, открывающую документ, который находится на сервере сети, в интрасети или в Интернете. |
|  INDEX | Uses an index to choose a value from a reference or array | ИНДЕКС | Использует индекс для выбора значения из ссылки или массива. |
|  ROW | Returns the row number of a reference | СТРОКА | Возвращает номер строки, определяемой ссылкой. |
|  ABS | Returns the absolute value of a number | ABS | Возвращает модуль (абсолютную величину) числа. |
|  ACOS | Returns the arccosine of a number | ACOS | Возвращает арккосинус числа. |
|  ACOSH | Returns the inverse hyperbolic cosine of a number | ACOSH | Возвращает гиперболический арккосинус числа. |
|  VLOOKUP | Looks in the first column of an array and moves across the row to return the value of a cell | ВПР | Ищет значение в первом столбце массива и возвращает значение из ячейки в найденной строке и указанном столбце. |
|  ROWS | Returns the number of rows in a reference | ЧСТРОК | Возвращает количество строк в ссылке. |
|  RTD | Retrieves real-time data from a program that supports COM automation | ДРВ | Получает данные реального времени из программы, поддерживающей автоматизацию COM. |
|  TRANSPOSE | Returns the transpose of an array | ТРАНСП | Возвращает транспонированный массив. |
|  LN | Returns the natural logarithm of a number | LN | Возвращает натуральный логарифм числа. |
|  LOG | Returns the logarithm of a number to a specified base | LOG | Возвращает логарифм числа по заданному основанию. |
|  LOG10 | Returns the base-10 logarithm of a number | LOG10 | Возвращает десятичный логарифм числа. |
|  LCM | Returns the least common multiple | НОК | Возвращает наименьшее общее кратное. |
|  GCD | Returns the greatest common divisor | НОД | Возвращает наибольший общий делитель. |
|  INT | Rounds a number down to the nearest integer | ЦЕЛОЕ | Округляет число до ближайшего меньшего целого. |
|  ISO.CEILING | Returns a number that is rounded up to the nearest integer or to the nearest multiple of significance | ISO.ОКРВВЕРХ | Округляет число в большую сторону до ближайшего целого или кратного. |
|  MDETERM | Returns the matrix determinant of an array | МОПРЕД | Возвращает определитель матрицы массива. |
|  MULTINOMIAL | Returns the multinomial of a set of numbers | МУЛЬТИНОМ | Возвращает мультиномиальный коэффициент множества чисел. |
|  MUNIT | Returns the unit matrix or the specified dimension | МЕДИН | Возвращает матрицу единицы или заданный размер. |
|  ODD | Rounds a number up to the nearest odd integer | НЕЧЁТ | Округляет число до ближайшего нечетного целого. |
|  MROUND | Returns a number rounded to the desired multiple | ОКРУГЛТ | Возвращает число, округленное с требуемой точностью. |
|  MINVERSE | Returns the matrix inverse of an array | МОБР | Возвращает обратную матрицу массива. |
|  MMULT | Returns the matrix product of two arrays | МУМНОЖ | Возвращает матричное произведение двух массивов. |
|  MOD | Returns the remainder from division | ОСТАТ | Возвращает остаток от деления. |
|  CSC | Returns the cosecant of an angle | CSC | Возвращает косеканс угла. |
|  CSCH | Returns the hyperbolic cosecant of an angle | CSCH | Возвращает гиперболический косеканс угла. |
|  DECIMAL | Converts a text representation of a number in a given base into a decimal number | ДЕС | Преобразует текстовое представление числа в заданном основании в десятичное число. |
|  COTH | Returns the hyperbolic cotangent of a number | COTH | Возвращает гиперболический котангенс числа. |
|  COS | Returns the cosine of a number | COS | Возвращает косинус числа. |
|  COSH | Returns the hyperbolic cosine of a number | COSH | Возвращает гиперболический косинус числа. |
|  COT | Returns the cotangent of an angle | COT | Возвращает котангенс угла. |
|  DEGREES | Converts radians to degrees | ГРАДУСЫ | Преобразует радианы в градусы. |
|  FLOOR | Rounds a number down, toward zero | ОКРВНИЗ | Округляет число до ближайшего меньшего по модулю значения. |
|  FLOOR.MATH | Rounds a number down, to the nearest integer or to the nearest multiple of significance | ОКРВНИЗ.МАТ | Округляет число в меньшую сторону до ближайшего целого или кратного. |
|  FLOOR.PRECISE | Rounds a number down to the nearest integer or to the nearest multiple of significance. Regardless of the sign of the number, the number is rounded down. | ОКРВНИЗ.ТОЧН | Округляет число в меньшую сторону (независимо от его знака) до ближайшего целого или кратного. |
|  FACTDOUBLE | Returns the double factorial of a number | ДВФАКТР | Возвращает двойной факториал числа. |
|  EVEN | Rounds a number up to the nearest even integer | ЧЁТН | Округляет число до ближайшего четного целого. |
|  EXP | Returns <br/>e raised to the power of a given number | EXP | Возвращает число e, возведенное в указанную степень. |
|  FACT | Returns the factorial of a number | ФАКТР | Возвращает факториал числа. |
|  F.INV | Returns the inverse of the F probability distribution | F.ОБР | Возвращает обратное значение для F-распределения вероятности. |
|  WEIBULL.DIST | Returns the Weibull distribution | ВЕЙБУЛЛ.РАСП | Возвращает распределение Вейбулла. |
|  Z.TEST | Returns the one-tailed probability-value of a z-test | Z.ТЕСТ | Возвращает одностороннее значение вероятности z-теста. |
|  ASC | Changes full-width (double-byte) English letters or katakana within a character string to half-width (single-byte) characters | ASC | Для языков с двухбайтовыми наборами знаков (например, катакана) преобразует полноширинные (двухбайтовые) знаки в полуширинные (однобайтовые). |
|  VARPA | Calculates variance based on the entire population, including numbers, text, and logical values | ДИСПРА | Вычисляет дисперсию для генеральной совокупности, включая числа, текст и логические значения. |
|  VAR.P | Calculates variance based on the entire population | ДИСП.Г | Вычисляет дисперсию по генеральной совокупности. |
|  VAR.S | Estimates variance based on a sample | ДИСП.В | Оценивает дисперсию по выборке. |
|  VARA | Estimates variance based on a sample, including numbers, text, and logical values | ДИСПА | Оценивает дисперсию по выборке, включая числа, текст и логические значения. |
|  BAHTTEXT | Converts a number to text, using the ß (baht) currency format | БАТТЕКСТ | Преобразует число в текст, используя денежный формат ß (БАТ). |
|  CONCATENATE | Joins several text items into one text item | СЦЕПИТЬ | Объединяет несколько текстовых элементов в один. |
|  DBCS | Changes half-width (single-byte) English letters or katakana within a character string to full-width (double-byte) characters | DBCS | Для языков с двухбайтовыми наборами знаков (например, катакана) преобразует полуширинные (однобайтовые) знаки в текстовой строке в полноширинные (двухбайтовые). |
|  DOLLAR | Converts a number to text, using the $ (dollar) currency format | РУБЛЬ | Преобразует число в текст, используя денежный формат рубля. |
|  CONCAT | Combines the text from multiple ranges and/or strings, but it doesn't provide the delimiter or IgnoreEmpty arguments. | СЦЕП | Объединяет текст из нескольких диапазонов или строк, но не добавляет разделитель или аргументы IgnoreEmpty. |
|  CHAR | Returns the character specified by the code number | СИМВОЛ | Возвращает символ с заданным кодом. |
|  CLEAN | Removes all nonprintable characters from text | ПЕЧСИМВ | Удаляет из текста все непечатаемые символы. |
|  CODE | Returns a numeric code for the first character in a text string | КОДСИМВ | Возвращает числовой код первого знака в текстовой строке. |
|  STDEVA | Estimates standard deviation based on a sample, including numbers, text, and logical values | СТАНДОТКЛОНА | Оценивает стандартное отклонение по выборке, включая числа, текст и логические значения. |
|  STDEVPA | Calculates standard deviation based on the entire population, including numbers, text, and logical values | СТАНДОТКЛОНПА | Вычисляет стандартное отклонение по генеральной совокупности, включая числа, текст и логические значения. |
|  STEYX | Returns the standard error of the predicted y-value for each x in the regression | СТОШYX | Возвращает стандартную ошибку предсказанных значений y для каждого значения x в регрессии. |
|  STDEV.S | Estimates standard deviation based on a sample | СТАНДОТКЛОН.В | Оценивает стандартное отклонение по выборке. |
|  SMALL | Returns the k-th smallest value in a data set | НАИМЕНЬШИЙ | Возвращает k-ое наименьшее значение в множестве данных. |
|  STANDARDIZE | Returns a normalized value | НОРМАЛИЗАЦИЯ | Возвращает нормализованное значение. |
|  STDEV.P | Calculates standard deviation based on the entire population | СТАНДОТКЛОН.Г | Вычисляет стандартное отклонение по генеральной совокупности. |
|  T.DIST | Returns the Percentage Points (probability) for the Student t-distribution | СТЬЮДРАСП | Возвращает процентные точки (вероятность) для t-распределения Стьюдента. |
|  T.TEST | Returns the probability associated with a Student's t-test | СТЬЮДЕНТ.ТЕСТ | Возвращает вероятность, соответствующую проверке по критерию Стьюдента. |
|  TREND | Returns values along a linear trend | ТЕНДЕНЦИЯ | Возвращает значения в соответствии с линейным трендом. |
|  TRIMMEAN | Returns the mean of the interior of a data set | УРЕЗСРЕДНЕЕ | Возвращает среднее внутренности множества данных. |
|  T.INV.2T | Returns the inverse of the Student's t-distribution | СТЬЮДЕНТ.ОБР.2Х | Возвращает обратное t-распределение Стьюдента. |
|  T.DIST.2T | Returns the Percentage Points (probability) for the Student t-distribution | СТЬЮДЕНТ.РАСП.2Х | Возвращает процентные точки (вероятность) для t-распределения Стьюдента. |
|  T.DIST.RT | Returns the Student's t-distribution | СТЬЮДЕНТ.РАСП.ПХ | Возвращает t-распределение Стьюдента. |
|  T.INV | Returns the t-value of the Student's t-distribution as a function of the probability and the degrees of freedom | СТЬЮДЕНТ.ОБР | Возвращает значение t для t-распределения Стьюдента как функцию вероятности и степеней свободы. |
|  UNICHAR | Returns the Unicode character that is references by the given numeric value | ЮНИСИМВ | Возвращает символ Юникод, на который ссылается заданное числовое значение. |
|  UNICODE | Returns the number (code point) that corresponds to the first character of the text | UNICODE | Возвращает число (кодовую страницу), которая соответствует первому символу текста. |
|  UPPER | Converts text to uppercase | ПРОПИСН | Преобразует все буквы текста в прописные. |
|  TRIM | Removes spaces from text | СЖПРОБЕЛЫ | Удаляет из текста пробелы. |
|  T | Converts its arguments to text | Т | Преобразует аргументы в текст. |
|  TEXT | Formats a number and converts it to text | ТЕКСТ | Форматирует число и преобразует его в текст. |
|  TEXTJOIN | Combines the text from multiple ranges and/or strings, and includes a delimiter you specify between each text value that will be combined. If the delimiter is an empty text string, this function will effectively concatenate the ranges. | ОБЪЕДИНИТЬ | Объединяет текст из нескольких диапазонов или строк, вставляя между текстовыми значениями указанный разделитель. Если в качестве разделителя используется пустая текстовая строка, функция эффективно объединит диапазоны. |
|  VALUE | Converts a text argument to a number | ЗНАЧЕН | Преобразует текстовый аргумент в число. |
|  ENCODEURL | Returns a URL-encoded string | КОДИР.URL | Возвращает строку запроса, зашифрованную в виде URL-адреса. |
|  FILTERXML | Returns specific data from the XML content by using the specified XPath | ФИЛЬТР.XML | Возвращает определенные данные из содержимого XML, используя указанный объект XPath. |
|  WEBSERVICE | Returns data from a web service | ВЕБСЛУЖБА | Возвращает данные из веб-службы. |
|  SQL.REQUEST | Connects with an external data source and runs a query from a worksheet, then returns the result as an array without the need for macro programming | SQL.REQUEST | Подключается к внешнему источнику данных и выполняет запрос из листа. Результат возвращается в виде массива. Дополнительное программирование не требуется. |
|  CALL | Calls a procedure in a dynamic link library or code resource | ВЫЗВАТЬ | Вызывает процедуру из динамически связываемой библиотеки или программный ресурс. |
|  EUROCONVERT | Converts a number to euros, converts a number from euros to a euro member currency, or converts a number from one euro member currency to another by using the euro as an intermediary (triangulation) | ПЕРЕСЧЕТЕВРО | Преобразует число в значение в единицах евро, преобразует значения в евро в значение в национальной валюте стран, использующих евро, или преобразует число из одной национальной валюты в другую, используя в качестве промежуточного результата значение в евро (триангуляция). |
|  REGISTER.ID | Returns the register ID of the specified dynamic link library (DLL) or code resource that has been previously registered | РЕГИСТРАТОР.ИД | Возвращает регистрационный идентификатор для указанной динамически связываемой библиотеки или ранее зарегистрированного программного ресурса. |
|  LEN, LENBs | Returns the number of characters in a text string | ДЛСТР, ДЛИНБ | Возвращают количество знаков в текстовой строке. |
|  LOWER | Converts text to lowercase | СТРОЧН | Преобразует все буквы текста в строчные. |
|  MID, MIDBs | Returns a specific number of characters from a text string starting at the position you specify | ПСТР, ПСТРБ | Возвращают заданное число знаков из строки текста, начиная с указанной позиции. |
|  LEFT, LEFTBs | Returns the leftmost characters from a text value | ЛЕВСИМВ, ЛЕВБ | Возвращают крайние слева знаки текстового значения. |
|  EXACT | Checks to see if two text values are identical | СОВПАД | Проверяет идентичность двух текстовых значений. |
|  FIND, FINDBs | Finds one text value within another (case-sensitive) | НАЙТИ, НАЙТИБ | Ищет вхождения одного текстового значения в другом (с учетом регистра). |
|  FIXED | Formats a number as text with a fixed number of decimals | ФИКСИРОВАННЫЙ | Форматирует число и преобразует его в текст с заданным числом десятичных знаков. |
|  NUMBERVALUE | Converts text to number in a locale-independent manner | ЧЗНАЧ | Преобразует текст в число независимо от языкового стандарта. |
|  RIGHT, RIGHTBs | Returns the rightmost characters from a text value | ПРАВСИМВ, ПРАВБ | Возвращают крайние справа знаки текстовой строки. |
|  SEARCH, SEARCHBs | Finds one text value within another (not case-sensitive) | ПОИСК, ПОИСКБ | Ищут вхождения одного текстового значения в другом (без учета регистра). |
|  SUBSTITUTE | Substitutes new text for old text in a text string | ПОДСТАВИТЬ | Заменяет в текстовой строке старый текст новым. |
|  REPT | Repeats text a<br/>given number of times | ПОВТОР | Повторяет текст заданное число раз. |
|  PHONETIC | Extracts the phonetic (furigana) characters from a text string | PHONETIC | Извлекает фонетические (фуригана) знаки из текстовой строки. |
|  PROPER | Capitalizes the first letter in each word of a text value | ПРОПНАЧ | Преобразует первую букву в каждом слове текста в прописную. |
|  REPLACE, REPLACEBs | Replaces characters within text | ЗАМЕНИТЬ, ЗАМЕНИТЬБ | Заменяют знаки в тексте. |
|  HARMEAN | Returns the harmonic mean | СРГАРМ | Возвращает среднее гармоническое. |
|  HYPGEOM.DIST | Returns the hypergeometric distribution | ГИПЕРГЕОМ.РАСП | Возвращает гипергеометрическое распределение. |
|  INTERCEPT | Returns the intercept of the linear regression line | ОТРЕЗОК | Возвращает отрезок, отсекаемый на оси линией линейной регрессии. |
|  GROWTH | Returns values along an exponential trend | РОСТ | Возвращает значения в соответствии с экспоненциальным трендом. |
|  GAMMALN.PRECISE | Returns the natural logarithm of the gamma function, Γ(x) | ГАММАНЛОГ.ТОЧН | Возвращает натуральный логарифм гамма-функции, Γ(x). |
|  GAUSS | Returns 0.5 less than the standard normal cumulative distribution | ГАУСС | Возвращает значение на 0,5 меньше стандартного нормального распределения. |
|  GEOMEAN | Returns the geometric mean | СРГЕОМ | Возвращает среднее геометрическое. |
|  KURT | Returns the kurtosis of a data set | ЭКСЦЕСС | Возвращает эксцесс множества данных. |
|  LOGNORM.INV | Returns the inverse of the lognormal cumulative distribution | ЛОГНОРМ.ОБР | Возвращает обратное значение интегрального логарифмического нормального распределения. |
|  MAX | Returns the maximum value in a list of arguments | МАКС | Возвращает наибольшее значение в списке аргументов. |
|  MAXA | Returns the maximum value in a list of arguments, including numbers, text, and logical values | МАКСА | Возвращает наибольшее значение в списке аргументов, включая числа, текст и логические значения. |
|  LOGNORM.DIST | Returns the cumulative lognormal distribution | ЛОГНОРМ.РАСП | Возвращает интегральное логарифмическое нормальное распределение. |
|  LARGE | Returns the k-th largest value in a data set | НАИБОЛЬШИЙ | Возвращает k-ое наибольшее значение в множестве данных. |
|  LINEST | Returns the parameters of a linear trend | ЛИНЕЙН | Возвращает параметры линейного тренда. |
|  LOGEST | Returns the parameters of an exponential trend | ЛГРФПРИБЛ | Возвращает параметры экспоненциального тренда. |
|  FORECAST | Returns a value along a linear trend<br/>                Note: In Excel 2016, this function is replaced with FORECAST.LINEAR as part of the new Forecasting functions, but it's  still available for compatibility with earlier versions. | ПРЕДСКАЗ | Возвращает значение линейного тренда.<br/>                Примечание. В Excel 2016 эта функция заменена функцией ПРЕДСКАЗ.ЛИНЕЙН из нового набора функций прогнозирования. Однако эта функция по-прежнему доступна в целях обеспечения совместимости с предыдущими версиями. |
|  FORECAST.ETS | Returns a future value based on existing (historical) values by using the AAA version of the Exponential Smoothing (ETS) algorithm<br/>                Note: This function isn't available in Excel 2016 for Mac. | ПРЕДСКАЗ.ETS | Возвращает будущее значение на основе существующих (ретроспективных) данных с использованием версии AAA алгоритма экспоненциального сглаживания (ETS).<br/>                Примечание. Эта функция недоступна в Excel 2016 для Mac. |
|  FORECAST.ETS.CONFINT | Returns a confidence interval for the forecast value at the specified target date<br/>                Note: This function isn't available in Excel 2016 for Mac. | ПРЕДСКАЗ.ЕTS.ДОВИНТЕРВАЛ | Возвращает доверительный интервал для прогнозной величины на указанную дату.<br/>                Примечание. Эта функция недоступна в Excel 2016 для Mac. |
|  FISHERINV | Returns the inverse of the Fisher transformation | ФИШЕРОБР | Возвращает обратное преобразование Фишера. |
|  F.INV.RT | Returns the inverse of the F probability distribution | F.ОБР.ПХ | Возвращает обратное значение для F-распределения вероятности. |
|  F.TEST | Returns the result of an F-test | F.ТЕСТ | Возвращает результат F-теста. |
|  FISHER | Returns the Fisher transformation | ФИШЕР | Возвращает преобразование Фишера. |
|  FORECAST.ETS.SEASONALITY | Returns the length of the repetitive pattern Excel detects for the specified time series<br/>                Note: This function isn't available in Excel 2016 for Mac. | ПРЕДСКАЗ.ETS.СЕЗОННОСТЬ | Возвращает длину повторяющегося фрагмента, обнаруженного программой Excel в заданном временном ряду.<br/>                Примечание. Эта функция недоступна в Excel 2016 для Mac. |
|  GAMMA.DIST | Returns the gamma distribution | ГАММА.РАСП | Возвращает гамма-распределение. |
|  GAMMA.INV | Returns the inverse of the gamma cumulative distribution | ГАММА.ОБР | Возвращает обратное значение интегрального гамма-распределения. |
|  GAMMALN | Returns the natural logarithm of the gamma function, Γ(x) | ГАММАНЛОГ | Возвращает натуральный логарифм гамма-функции, Γ(x). |
|  GAMMA | Returns the Gamma function value | ГАММА | Возвращает значение функции гамма. |
|  FORECAST.ETS.STAT | Returns a statistical value as a result of time series forecasting<br/>                Note: This function isn't available in Excel 2016 for Mac. | ПРЕДСКАЗ.ETS.СТАТ | Возвращает статистическое значение, являющееся результатом прогнозирования временного ряда.<br/>                Примечание. Эта функция недоступна в Excel 2016 для Mac. |
|  FORECAST.LINEAR | Returns a future value based on existing values<br/>                Note: This function isn't available in Excel 2016 for Mac. | ПРЕДСКАЗ.ЛИНЕЙН | Возвращает будущее значение на основе существующих значений.<br/>                Примечание. Эта функция недоступна в Excel 2016 для Mac. |
|  FREQUENCY | Returns a frequency distribution as a vertical array | ЧАСТОТА | Возвращает распределение частот в виде вертикального массива. |
|  PHI | Returns the value of the density function for a standard normal distribution | ФИ | Возвращает значение функции плотности для стандартного нормального распределения. |
|  POISSON.DIST | Returns the Poisson distribution | ПУАССОН.РАСП | Возвращает распределение Пуассона. |
|  PROB | Returns the probability that values in a range are between two limits | ВЕРОЯТНОСТЬ | Возвращает вероятность того, что значение из диапазона находится внутри заданных пределов. |
|  PERMUTATIONA | Returns the number of permutations for a given number of objects (with repetitions) that can be selected from the total objects | ПЕРЕСТА | Возвращает количество перестановок для заданного числа объектов (с повторами), которые можно выбрать из общего числа объектов. |
|  PERCENTRANK.EXC | Returns the rank of a value in a data set as a percentage (0..1, exclusive) of the data set | ПРОЦЕНТРАНГ.ИСКЛ | Возвращает ранг значения в наборе данных как процентную долю набора (от 0 до 1, исключая границы). |
|  PERCENTRANK.INC | Returns the percentage rank of a value in a data set | ПРОЦЕНТРАНГ.ВКЛ | Возвращает процентную норму значения в наборе данных. |
|  PERMUT | Returns the number of permutations for a given number of objects | ПЕРЕСТ | Возвращает количество перестановок для заданного числа объектов. |
|  QUARTILE.EXC | Returns the quartile of the data set, based on percentile values from 0..1, exclusive | КВАРТИЛЬ.ИСКЛ | Возвращает квартиль набора данных на основе значений процентили из диапазона от 0 до 1, исключая границы. |
|  SKEW | Returns the skewness of a distribution | СКОС | Возвращает асимметрию распределения. |
|  SKEW.P | Returns the skewness of a distribution based on a population: a characterization of the degree of asymmetry of a distribution around its mean | СКОС.Г | Возвращает асимметрию распределения на основе заполнения: характеристика степени асимметрии распределения относительно его среднего. |
|  SLOPE | Returns the slope of the linear regression line | НАКЛОН | Возвращает наклон линии линейной регрессии. |
|  RSQ | Returns the square of the Pearson product moment correlation coefficient | КВПИРСОН | Возвращает квадрат коэффициента корреляции Пирсона. |
|  QUARTILE.INC | Returns the quartile of a data set | КВАРТИЛЬ.ВКЛ | Возвращает квартиль набора данных. |
|  RANK.AVG | Returns the rank of a number in a list of numbers | РАНГ.СР | Возвращает ранг числа в списке чисел. |
|  RANK.EQ | Returns the rank of a number in a list of numbers | РАНГ.РВ | Возвращает ранг числа в списке чисел. |
|  MINIFS | Returns the minimum value among cells specified by a given set of conditions or criteria. | МИНЕСЛИ | Возвращает минимальное значение из заданных определенными условиями или критериями ячеек. |
|  MODE.MULT | Returns a vertical array of the most frequently occurring, or repetitive values in an array or range of data | МОДА.НСК | Возвращает вертикальный массив наиболее часто встречающихся или повторяющихся значений в массиве или диапазоне данных. |
|  MODE.SNGL | Returns the most common value in a data set | МОДА.ОДН | Возвращает значение моды набора данных. |
|  MINA | Returns the smallest value in a list of arguments, including numbers, text, and logical values | МИНА | Возвращает наименьшее значение в списке аргументов, включая числа, текст и логические значения. |
|  MAXIFS | Returns the maximum value among cells specified by a given set of conditions or criteria | МАКСЕСЛИ | Возвращает максимальное значение из заданных определенными условиями или критериями ячеек. |
|  MEDIAN | Returns the median of the given numbers | МЕДИАНА | Возвращает медиану заданных чисел. |
|  MIN | Returns the minimum value in a list of arguments | МИН | Возвращает наименьшее значение в списке аргументов. |
|  NEGBINOM.DIST | Returns the negative binomial<br/>distribution | ОТРБИНОМ.РАСП | Возвращает отрицательное биномиальное распределение. |
|  PEARSON | Returns the Pearson product moment correlation coefficient | ПИРСОН | Возвращает коэффициент корреляции Пирсона. |
|  PERCENTILE.EXC | Returns the k-th percentile of values in a range, where k is in the range 0..1, exclusive | ПРОЦЕНТИЛЬ.ИСКЛ | Возвращает k-ю процентиль значений в диапазоне, где k может принимать значения от 0 до 1, исключая границы. |
|  PERCENTILE.INC | Returns the k-th percentile of values in a range | ПРОЦЕНТИЛЬ.ВКЛ | Возвращает k-ю процентиль для значений диапазона. |
|  NORM.S.INV | Returns the inverse of the standard normal cumulative distribution | НОРМ.СТ.ОБР | Возвращает обратное значение стандартного нормального интегрального распределения. |
|  NORM.DIST | Returns the normal cumulative distribution | НОРМ.РАСП | Возвращает нормальное интегральное распределение. |
|  NORM.INV | Returns the inverse of the normal cumulative distribution | НОРМ.ОБР | Возвращает обратное значение нормального интегрального распределения. |
|  NORM.S.DIST | Returns the standard normal cumulative distribution | НОРМ.СТ.РАСП | Возвращает стандартное нормальное интегральное распределение. |
|  GETPIVOTDATA | Returns data stored in a PivotTable report | ПОЛУЧИТЬ.ДАННЫЕ.СВОДНОЙ.ТАБЛИЦЫ | Возвращает данные, хранящиеся в отчете сводной таблицы. |
|  ISOWEEKNUM | Returns the number of the ISO week number of the year for a given date | НОМНЕДЕЛИ.ISO | Возвращает номер недели по ISO для заданной даты. |
|  MINUTE | Converts a serial number to a minute | МИНУТЫ | Преобразует дату в числовом формате в минуты. |
|  MONTH | Converts a serial number to a month | МЕСЯЦ | Преобразует дату в числовом формате в месяцы. |
|  HOUR | Converts a serial number to an hour | ЧАС | Преобразует дату в числовом формате в часы. |
|  DAYS360 | Calculates the number of days between two dates based on a 360-day year | ДНЕЙ360 | Вычисляет количество дней между двумя датами на основе 360-дневного года. |
|  EDATE | Returns the serial number of the date that is the indicated number of months before or after the start date | ДАТАМЕС | Возвращает дату в числовом формате, отстоящую на заданное число месяцев вперед или назад от начальной даты. |
|  EOMONTH | Returns the serial number of the last day of the month before or after a specified number of months | КОНМЕСЯЦА | Возвращает дату в числовом формате для последнего дня месяца, отстоящего вперед или назад на заданное число месяцев. |
|  NETWORKDAYS | Returns the number of whole workdays between two dates | ЧИСТРАБДНИ | Возвращает количество полных рабочих дней между двумя датами. |
|  TIMEVALUE | Converts a time in the form of text to a serial number | ВРЕМЗНАЧ | Преобразует время из текстового формата в числовой. |
|  TODAY | Returns the serial number of today's date | СЕГОДНЯ | Возвращает текущую дату в числовом формате. |
|  WEEKDAY | Converts a serial number to a day of the week | ДЕНЬНЕД | Преобразует дату в числовом формате в день недели. |
|  TIME | Returns the serial number of a particular time | ВРЕМЯ | Возвращает заданное время в числовом формате. |
|  NETWORKDAYS.INTL | Returns the number of whole workdays between two dates using parameters to indicate which and how many days are weekend days | ЧИСТРАБДНИ.МЕЖД | Возвращает количество полных рабочих дней в интервале между двумя датами, руководствуясь параметрами, указывающими выходные дни и их количество. |
|  NOW | Returns the serial number of the current date and time | ТДАТА | Возвращает текущую дату и время в числовом формате. |
|  SECOND | Converts a serial number to a second | СЕКУНДЫ | Преобразует дату в числовом формате в секунды. |
|  DPRODUCT | Multiplies the values in a particular field of records that match the criteria in a database | БДПРОИЗВЕД | Перемножает значения определенного поля в записях базы данных, удовлетворяющих условию. |
|  DSTDEV | Estimates the standard deviation based on a sample of selected database entries | ДСТАНДОТКЛ | Оценивает стандартное отклонение по выборке из выделенных записей базы данных. |
|  DSTDEVP | Calculates the standard deviation based on the entire population of selected database entries | ДСТАНДОТКЛП | Вычисляет стандартное отклонение по генеральной совокупности из выделенных записей базы данных. |
|  DMIN | Returns the minimum value from selected database entries | ДМИН | Возвращает минимальное значение среди выделенных записей базы данных. |
|  DCOUNTA | Counts nonblank cells in a database | БСЧЁТА | Подсчитывает количество непустых ячеек в базе данных. |
|  DGET | Extracts from a database a single record that matches the specified criteria | БИЗВЛЕЧЬ | Извлекает из базы данных одну запись, удовлетворяющую заданному условию. |
|  DMAX | Returns the maximum value from selected database entries | ДМАКС | Возвращает максимальное значение среди выделенных записей базы данных. |
|  DSUM | Adds the numbers in the field column of records in the database that match the criteria | БДСУММ | Суммирует числа в поле для записей базы данных, удовлетворяющих условию. |
|  DATEVALUE | Converts a date in the form of text to a serial number | ДАТАЗНАЧ | Преобразует дату из текстового формата в числовой. |
|  DAY | Converts a serial number to a day of the month | ДЕНЬ | Преобразует дату в числовом формате в день месяца. |
|  DAYS | Returns the number of days between two dates | ДНИ | Возвращает количество дней между двумя датами. |
|  DATEDIF | Calculates the number of days, months, or years between two dates. This  function is useful in formulas where you need to calculate an age. | РАЗНДАТ | Вычисляет количество дней, месяцев или лет между двумя датами. Эта функция полезна в формулах расчета возраста. |
|  DVAR | Estimates variance based on a sample from selected database entries | БДДИСП | Оценивает дисперсию по выборке из выделенных записей базы данных. |
|  DVARP | Calculates variance based on the entire population of selected database entries | БДДИСПП | Вычисляет дисперсию по генеральной совокупности для выделенных записей базы данных. |
|  DATE | Returns the serial number of a particular date | ДАТА | Возвращает заданную дату в числовом формате. |
|  DEC2BIN | Converts a decimal number to binary | ДЕС.В.ДВ | Преобразует десятичное число в двоичное. |
|  DEC2HEX | Converts a decimal number to hexadecimal | ДЕС.В.ШЕСТН | Преобразует десятичное число в шестнадцатеричное. |
|  DEC2OCT | Converts a decimal number to octal | ДЕС.В.ВОСЬМ | Преобразует десятичное число в восьмеричное. |
|  CONVERT | Converts a number from one measurement system to another | ПРЕОБР | Преобразует число из одной системы счисления в другую. |
|  BITRSHIFT | Returns a value number shifted right by shift_amount bits | БИТ.СДВИГП | Возвращает число со сдвигом вправо на указанное число бит. |
|  BITXOR | Returns a bitwise 'Exclusive Or' of two numbers | БИТ.ИСКЛИЛИ | Возвращает побитовое "исключающее или" двух чисел. |
|  COMPLEX | Converts real and imaginary coefficients into a complex number | КОМПЛЕКСН | Преобразует коэффициенты при вещественной и мнимой частях комплексного числа в комплексное число. |
|  DELTA | Tests whether two values are equal | ДЕЛЬТА | Проверяет равенство двух значений. |
|  GESTEP | Tests whether a number is greater than a threshold value | ПОРОГ | Проверяет, не превышает ли данное число пороговое значение. |
|  HEX2BIN | Converts a hexadecimal number to binary | ШЕСТН.В.ДВ | Преобразует шестнадцатеричное число в двоичное. |
|  HEX2DEC | Converts a hexadecimal number to decimal | ШЕСТН.В.ДЕС | Преобразует шестнадцатеричное число в десятичное. |
|  ERFC.PRECISE | Returns the complementary ERF function integrated between<br/>x and infinity | ДФОШ.ТОЧН | Возвращает дополнительную функцию ФОШ, проинтегрированную в пределах от x до бесконечности. |
|  ERF | Returns the error function | ФОШ | Возвращает функцию ошибки. |
|  ERF.PRECISE | Returns the error function | ФОШ.ТОЧН | Возвращает функцию ошибки. |
|  ERFC | Returns the complementary error function | ДФОШ | Возвращает дополнительную функцию ошибки. |
|  YEARFRAC | Returns the year fraction representing the number of whole days between start_date and end_date | ДОЛЯГОДА | Возвращает долю года, которую составляет количество дней между начальной и конечной датами. |
|  BESSELI | Returns the modified Bessel function In(x) | БЕССЕЛЬ.I | Возвращает модифицированную функцию Бесселя In(x). |
|  BESSELJ | Returns the Bessel function Jn(x) | БЕССЕЛЬ.J | Возвращает функцию Бесселя Jn(x). |
|  YEAR | Converts a serial number to a year | ГОД | Преобразует дату в числовом формате в год. |
|  WEEKNUM | Converts a serial number to a number representing where the week falls numerically with a<br/>year | НОМНЕДЕЛИ | Преобразует дату в числовом формате в число, которое указывает, на какую неделю года приходится дата. |
|  WORKDAY | Returns the serial number of the date before or after a specified number of workdays | РАБДЕНЬ | Возвращает дату в числовом формате, отстоящую вперед или назад на заданное количество рабочих дней. |
|  WORKDAY.INTL | Returns the serial number of the date before or after a specified number of workdays using parameters to indicate which and how many days are weekend days | РАБДЕНЬ.МЕЖД | Возвращает числовое значение даты, предшествующей заданному количеству рабочих дней или следующей за ними, руководствуясь при этом параметрами, указывающими выходные дни и их количество. |
|  BESSELK | Returns the modified Bessel function Kn(x) | БЕССЕЛЬ.K | Возвращает модифицированную функцию Бесселя Kn(x). |
|  BITAND | Returns a 'Bitwise And' of two numbers | БИТ.И | Возвращает результат операции поразрядного И для двух чисел. |
|  BITLSHIFT | Returns a value number shifted left by shift_amount bits | БИТ.СДВИГЛ | Возвращает число со сдвигом влево на указанное число бит. |
|  BITOR | Returns a bitwise OR of 2 numbers | БИТ.ИЛИ | Возвращает результат операции поразрядного ИЛИ для двух чисел. |
|  BIN2OCT | Converts a binary number to octal | ДВ.В.ВОСЬМ | Преобразует двоичное число в восьмеричное. |
|  BESSELY | Returns the Bessel function Yn(x) | БЕССЕЛЬ.Y | Возвращает функцию Бесселя Yn(x). |
|  BIN2DEC | Converts a binary number to decimal | ДВ.В.ДЕС | Преобразует двоичное число в десятичное. |
|  BIN2HEX | Converts a binary number to hexadecimal | ДВ.В.ШЕСТН | Преобразует двоичное число в шестнадцатеричное. |
|  CRITBINOM | Returns the smallest value for which the cumulative binomial distribution is less than or equal to a criterion value | КРИТБИНОМ | Возвращает наименьшее значение, для которого интегральное биномиальное распределение меньше заданного значения или равно ему. |
|  EXPONDIST | Returns the exponential distribution | ЭКСПРАСП | Возвращает экспоненциальное распределение. |
|  FDIST | Returns the F probability distribution | FРАСП | Возвращает F-распределение вероятности. |
|  COVAR | Returns covariance, the average of the products of paired deviations | КОВАР | Возвращает ковариацию, среднее произведений парных отклонений. |
|  CHITEST | Returns the test for independence | ХИ2ТЕСТ | Возвращает тест на независимость. |
|  CONCATENATE | Joins two or more text strings into one string | СЦЕПИТЬ | Соединяет несколько текстовых строк в одну строку. |
|  CONFIDENCE | Returns the confidence interval for a population mean | ДОВЕРИТ | Возвращает доверительный интервал для среднего значения по генеральной совокупности. |
|  FINV | Returns the inverse of the F probability distribution | FРАСПОБР | Возвращает обратное значение для F-распределения вероятности. |
|  GAMMAINV | Returns the inverse of the gamma cumulative distribution | ГАММАОБР | Возвращает обратное значение интегрального гамма-распределения. |
|  HYPGEOMDIST | Returns the hypergeometric distribution | ГИПЕРГЕОМЕТ | Возвращает гипергеометрическое распределение. |
|  LOGINV | Returns the inverse of the lognormal cumulative distribution function | ЛОГНОРМОБР | Возвращает обратное значение интегрального логарифмического нормального распределения. |
|  GAMMADIST | Returns the gamma distribution | ГАММАРАСП | Возвращает гамма-распределение. |
|  FLOOR | Rounds a number down, toward zero | ОКРВНИЗ | Округляет число до ближайшего меньшего по модулю значения. |
|  FORECAST | Calculates, or predicts, a future value by using existing values. | ПРЕДСКАЗ | Вычисляет, или прогнозирует, будущее значение по существующим значениям. |
|  FTEST | Returns the result of an F-test | ФТЕСТ | Возвращает результат F-теста. |
|  MATCH | Use this function to  search for an item in a range of cells, and then return the relative position of that item in the range. For example, if the range A1:A3 contains the values 5, 7, and 38, then the formula<br/><br/>=MATCH(7,A1:A3,0) <br/><br/>returns the number 2, because 7 is the second item in the range. | ПОИСКПОЗ | Эта функция выполняет поиск указанного элемента в диапазоне ячеек и возвращает его относительную позицию в диапазоне. Например, если диапазон A1:A3 содержит значения 5, 7 и 38, то формула<br/><br/>=ПОИСКПОЗ(7,A1:A3,0)<br/><br/>возвращает значение 2, поскольку элемент 7 является вторым в диапазоне. |
|  CHOOSE | Use this function to select one of up to 254 values based on the index number. For example, if value1 through value7 are the days of the week, CHOOSE returns one of the days when a number between 1 and 7 is used as index_num. | ВЫБОР | Эта функция позволяет выбрать одно значение из списка, в котором может быть до 254 значений. Например, если первые семь значений — это дни недели, то функция ВЫБОР возвращает один из дней при использовании числа от 1 до 7 в качестве аргумента "номер_индекса". |
|  DATE | Use this function to return the sequential serial number that represents a particular date. This function is most useful in situations where the year, month, and day are supplied by formulas or cell references. For example, you might have a worksheet that contains dates in a format that Excel does not recognize, such as YYYYMMDD.<br/>                Use the DATEDIF function to calculate the number of days, months, or years between two dates. | ДАТА | Эта функция возвращает порядковый номер определенной даты. Эта функция особенно полезна в ситуациях, когда значения года, месяца и дня возвращаются формулами или ссылками на ячейки. Предположим, у вас есть лист с датами в формате, который Excel не распознает, например ГГГГММДД.<br/>                Функция РАЗНДАТ вычисляет количество дней, месяцев или лет между двумя датами. |
|  VLOOKUP | Use this function when you need to find things in a table or a range by row. For example, look up an employee's last name by her employee number, or find her phone number by looking up her last name (just like a telephone book). Check out this video about using VLOOKUP. | ВПР | Эта функция используется для поиска данных в таблице или диапазоне по строкам. Например, можно найти фамилию сотрудника по его номеру или его номер телефона по фамилии (как в телефонной книге). Посмотрите это видео об использовании функции ВПР. |
|  SUM | Use this function to add the values in cells. | СУММ | Эта функция используется для суммирования значений в ячейках. |
|  IF | Use this function to return one value if a condition is true and another value if it's false. Here's a video about using the IF function. | ЕСЛИ | Эта функция возвращает разные значения в зависимости от того, соблюдается ли условие. Вот видео об использовании функции ЕСЛИ. |
|  LOOKUP | Use this function when you need to look in a single row or column and find a value from the same position in a second row or column. | ПРОСМОТР | Используйте эту функцию, когда нужно взять определенную строку или столбец и найти значение, находящееся в той же позиции во второй строке или столбце. |
|  DAYS | Use this function to return the number of days between two dates. | ДНИ | Эта функция возвращает число дней между двумя датами. |
|  BINOMDIST | Returns the individual term binomial distribution probability | БИНОМРАСП | Возвращает отдельное значение вероятности биномиального распределения. |
|  CHIDIST | Returns the one-tailed probability of the chi-squared distribution | ХИ2РАСП | Возвращает одностороннюю вероятность распределения хи-квадрат. |
|  CHIINV | Returns the inverse of the one-tailed probability of the chi-squared distribution | ХИ2ОБР | Возвращает обратное значение односторонней вероятности распределения хи-квадрат. |
|  BETAINV | Returns the inverse of the cumulative distribution function for a specified beta distribution | БЕТАОБР | Возвращает обратную интегральную функцию указанного бета-распределения. |
|  FIND, FINDBs | FIND and FINDB locate one text string within a second text string. They return the number of the starting position of the first text string from the first character of the second text string. | НАЙТИ, НАЙТИБ | Функции НАЙТИ и НАЙТИБ находят вхождение одной текстовой строки в другую и возвращают начальную позицию искомой строки относительно первого знака второй. |
|  INDEX | Use this function to return a value or the reference to a value from within a table or range. | ИНДЕКС | Эта функция возвращает значение или ссылку на него из таблицы или диапазона. |
|  BETADIST | Returns the beta cumulative distribution function | БЕТАРАСП | Возвращает интегральную функцию бета-распределения. |
|  WEIBULL | Returns the Weibull distribution | ВЕЙБУЛЛ | Возвращает распределение Вейбулла. |
|  ZTEST | Returns the one-tailed probability-value of a z-test | ZТЕСТ | Возвращает одностороннее P-значение z-теста. |
|  CUBEKPIMEMBER | Returns a key performance indicator (KPI) property and displays the KPI name in the cell. A KPI is a quantifiable measurement, such as monthly gross profit or quarterly employee turnover, that is used to monitor an organization's performance. | КУБЭЛЕМЕНТКИП | Возвращает свойство ключевого показателя эффективности (КПЭ) и отображает его имя в ячейке. КПЭ представляет собой количественную величину, такую как ежемесячная валовая прибыль или ежеквартальная текучесть кадров, используемой для контроля эффективности работы организации. |
|  VARP | Calculates variance based on the entire population | ДИСПР | Вычисляет дисперсию по генеральной совокупности. |
|  TINV | Returns the inverse of the Student's t-distribution | СТЬЮДРАСПОБР | Возвращает обратное t-распределение Стьюдента. |
|  TTEST | Returns the probability associated with a Student's t-test | ТТЕСТ | Возвращает вероятность, соответствующую проверке по критерию Стьюдента. |
|  VAR | Estimates variance based on a sample | ДИСП | Оценивает дисперсию по выборке. |
|  CUBEMEMBER | Returns a member or tuple from the cube. Use to validate that the member or tuple exists in the cube. | КУБЭЛЕМЕНТ | Возвращает элемент или кортеж из куба. Используется для проверки существования элемента или кортежа в кубе. |
|  CUBEVALUE | Returns an aggregated value from the cube. | КУБЗНАЧЕНИЕ | Возвращает агрегированное значение из куба. |
|  DAVERAGE | Returns the average of selected database entries | ДСРЗНАЧ | Возвращает среднее значение выбранных записей базы данных. |
|  DCOUNT | Counts the cells that contain numbers in a database | БСЧЁТ | Подсчитывает количество числовых ячеек в базе данных. |
|  CUBESETCOUNT | Returns the number of items in a set. | КУБЧИСЛОЭЛМНОЖ | Возвращает число элементов в множестве. |
|  CUBEMEMBERPROPERTY | Returns the value of a member property from the cube. Use to validate that a member name exists within the cube and to return the specified property for this member. | КУБСВОЙСТВОЭЛЕМЕНТА | Возвращает число элементов множества. |
|  CUBERANKEDMEMBER | Returns the nth, or ranked, member in a set. Use to return one or more elements in a set, such as the top sales performer or the top 10 students. | КУБПОРЭЛЕМЕНТ | Возвращает n-й, или ранжированный, элемент в множестве. Используется для возвращения одного или нескольких элементов в множестве, например лучшего продавца или 10 лучших студентов. |
|  CUBESET | Defines a calculated set of members or tuples by sending a set expression to the cube on the server, which creates the set, and then returns that set to Microsoft Office Excel. | КУБМНОЖ | Определяет вычисленное множество элементов или кортежей путем пересылки установленного выражения в куб на сервере, который формирует множество, а затем возвращает его в Microsoft Office Excel. |
|  NORMINV | Returns the inverse of the normal cumulative distribution | НОРМОБР | Возвращает обратное значение нормального интегрального распределения. |
|  NORMSDIST | Returns the standard normal cumulative distribution | НОРМСТРАСП | Возвращает стандартное нормальное интегральное распределение. |
|  NORMSINV | Returns the inverse of the standard normal cumulative distribution | НОРМСТОБР | Возвращает обратное значение стандартного нормального интегрального распределения. |
|  NORMDIST | Returns the normal cumulative distribution | НОРМРАСП | Возвращает нормальное интегральное распределение. |
|  LOGNORMDIST | Returns the cumulative lognormal distribution | ЛОГНОРМРАСП | Возвращает интегральное логарифмическое нормальное распределение. |
|  MODE | Returns the most common value in a data set | МОДА | Возвращает значение моды набора данных. |
|  NEGBINOMDIST | Returns the negative binomial distribution | ОТРБИНОМРАСП | Возвращает отрицательное биномиальное распределение. |
|  PERCENTILE | Returns the k-th percentile of values in a range | ПЕРСЕНТИЛЬ | Возвращает k-ю процентиль для значений диапазона. |
|  STDEV | Estimates standard deviation based on a sample | СТАНДОТКЛОН | Оценивает стандартное отклонение по выборке. |
|  STDEVP | Calculates standard deviation based on the entire population | СТАНДОТКЛОНП | Вычисляет стандартное отклонение по генеральной совокупности. |
|  TDIST | Returns the Student's t-distribution | СТЬЮДРАСП | Возвращает t-распределение Стьюдента. |
|  RANK | Returns the rank of a number in a list of numbers | РАНГ | Возвращает ранг числа в списке чисел. |
|  PERCENTRANK | Returns the percentage rank of a value in a data set | ПРОЦЕНТРАНГ | Возвращает процентную норму значения в наборе данных. |
|  POISSON | Returns the Poisson distribution | ПУАССОН | Возвращает распределение Пуассона. |
|  QUARTILE | Returns the quartile of a data set | КВАРТИЛЬ | Возвращает квартиль набора данных. |
|  HEX2OCT | Converts a hexadecimal number to octal | ШЕСТН.В.ВОСЬМ | Преобразует шестнадцатеричное число в восьмеричное. |
|  XNPV | Returns the net present value for a schedule of cash flows that is not necessarily periodic | ЧИСТНЗ | Возвращает чистую приведенную стоимость для денежных потоков, не обязательно носящих периодический характер. |
|  YIELD | Returns the yield on a security that pays periodic interest | ДОХОД | Возвращает доход по ценным бумагам, по которым производятся периодические выплаты процентов. |
|  YIELDDISC | Returns the annual yield for a discounted security; for example, a Treasury bill | ДОХОДСКИДКА | Возвращает годовой доход по ценным бумагам, на которые сделана скидка (например, по казначейским векселям). |
|  XIRR | Returns the internal rate of return for a schedule of cash flows that is not necessarily periodic | ЧИСТВНДОХ | Возвращает внутреннюю ставку доходности для графика денежных потоков, не обязательно носящих периодический характер. |
|  TBILLPRICE | Returns the price per $100 face value for a Treasury bill | ЦЕНАКЧЕК | Возвращает цену за 100 рублей номинальной стоимости для казначейского векселя. |
|  TBILLYIELD | Returns the yield for a Treasury bill | ДОХОДКЧЕК | Возвращает доходность по казначейскому векселю. |
|  VDB | Returns the depreciation of an asset for a specified or partial period by using a declining balance method | ПУО | Возвращает величину амортизации актива для указанного или частичного периода при использовании метода сокращающегося баланса. |
|  YIELDMAT | Returns the annual yield of a security that pays interest at maturity | ДОХОДПОГАШ | Возвращает годовую доходность ценных бумаг, по которым процент выплачивается в строк погашения. |
|  ISERR | Returns TRUE if the value is any error value except #N/A | ЕОШ | Возвращает значение ИСТИНА, если аргумент ссылается на любое значение ошибки, кроме #Н/Д. |
|  ISERROR | Returns TRUE if the value is any error value | ЕОШИБКА | Возвращает значение ИСТИНА, если аргумент ссылается на любое значение ошибки. |
|  ISEVEN | Returns TRUE if the number is even | ЕЧЁТН | Возвращает значение ИСТИНА, если значение аргумента является четным числом. |
|  ISBLANK | Returns TRUE if the value is blank | ЕПУСТО | Возвращает значение ИСТИНА, если аргумент является ссылкой на пустую ячейку. |
|  CELL | Returns information about the formatting, location, or contents of a cell | ЯЧЕЙКА | Возвращает сведения о формате, расположении или содержимом ячейки. |
|  ERROR.TYPE | Returns a number corresponding to an error type | ТИП.ОШИБКИ | Возвращает числовой код, соответствующий типу ошибки. |
|  INFO | Returns information about the current operating environment<br/>                Note: This function is not available in Excel Online. | ИНФОРМ | Возвращает сведения о текущей операционной среде.<br/>                Примечание. Эта функция недоступна в Excel Online. |
|  PPMT | Returns the payment on the principal for an investment for a given period | ОСПЛТ | Возвращает платеж с основного вложенного капитала за данный период. |
|  PRICE | Returns the price per $100 face<br/>value of a security that pays periodic interest | ЦЕНА | Возвращает цену за 100 рублей номинальной стоимости ценных бумаг, по которым выплачивается периодический процент. |
|  PRICEDISC | Returns the price per $100 face value of a discounted security | ЦЕНАСКИДКА | Возвращает цену за 100 рублей номинальной стоимости ценных бумаг, на которые сделана скидка. |
|  PMT | Returns the periodic payment for an annuity | ПЛТ | Возвращает регулярный платеж годичной ренты. |
|  ODDLPRICE | Returns the price per $100 face value of a security with an odd last period | ЦЕНАПОСЛНЕРЕГ | Возвращает цену за 100 рублей номинальной стоимости ценных бумаг с нерегулярным (коротким или длинным) последним периодом купона. |
|  ODDLYIELD | Returns the yield of a security with an odd last period | ДОХОДПОСЛНЕРЕГ | Возвращает доход по ценным бумагам с нерегулярным (коротким или длинным) последним периодом купона. |
|  PDURATION | Returns the number of periods required by an investment to reach a specified value | ПДЛИТ | Возвращает количество периодов, необходимых инвестиции для достижения заданного значения. |
|  PRICEMAT | Returns the price per $100 face value of a security that pays interest at maturity | ЦЕНАПОГАШ | Возвращает цену за 100 рублей номинальной стоимости ценных бумаг, по которым процент выплачивается в срок погашения. |
|  SLN | Returns the straight-line depreciation of an asset for one period | АПЛ | Возвращает величину амортизации актива за один период, рассчитанную линейным методом. |
|  SYD | Returns the sum-of-years' digits depreciation of an asset for a specified period | АСЧ | Возвращает величину амортизации актива за данный период, рассчитанную методом суммы годовых чисел. |
|  TBILLEQ | Returns the bond-equivalent yield for a Treasury bill | РАВНОКЧЕК | Возвращает эквивалентный облигации доход по казначейскому векселю. |
|  RRI | Returns an equivalent interest rate for the growth of an investment | ЭКВ.СТАВКА | Возвращает эквивалентную процентную ставку для роста инвестиции. |
|  PV | Returns the present value of an investment | ПС | Возвращает приведенную (к текущему моменту) стоимость инвестиции. |
|  RATE | Returns the interest rate per period of an annuity | СТАВКА | Возвращает процентную ставку по аннуитету за один период. |
|  RECEIVED | Returns the amount received at maturity for a fully invested security | ПОЛУЧЕНО | Возвращает сумму, полученную к сроку погашения полностью инвестированных ценных бумаг. |
|  NOT | Reverses the logic of its argument | НЕ | Меняет логическое значение своего аргумента на противоположное. |
|  OR | Returns TRUE if any argument is TRUE | ИЛИ | Возвращает значение ИСТИНА, если хотя бы один аргумент имеет значение ИСТИНА. |
|  SWITCH | Evaluates an expression against a list of values and returns the result corresponding to the first matching value. If there is no match, an optional default value may be returned. | ПЕРЕКЛЮЧ | Сравнивает выражение со списком значений и возвращает результат, соответствующий первому совпадающему значению. Если совпадений не выявлено, может возвращаться указанное значение по умолчанию. |
|  IFS | Checks whether one or more conditions are met and returns a value that corresponds to the first TRUE condition. | УСЛОВИЯ | Проверяет соответствие одному или нескольким условиям и возвращает значение для первого условия, принимающего значение ИСТИНА. |
|  IF | Specifies a logical test to perform | ЕСЛИ | Выполняет проверку условия. |
|  IFERROR | Returns a value you specify if a formula evaluates to an error; otherwise, returns the result of the formula | ЕСЛИОШИБКА | Возвращает введенное значение, если вычисление по формуле вызывает ошибку; в противном случае возвращает результат вычисления. |
|  IFNA | Returns the value you specify if the expression resolves to #N/A, otherwise returns the result of the expression | ЕСНД | Возвращает значение, которое задается, если выражение принимает значение #Н/Д. В противном случае возвращает результат выражения. |
|  true | Returns the logical value TRUE | ИСТИНА | Возвращает логическое значение ИСТИНА. |
|  COLUMN | Returns the column number of a reference | СТОЛБЕЦ | Возвращает номер столбца, на который указывает ссылка. |
|  COLUMNS | Returns the number of columns in a reference | ЧИСЛСТОЛБ | Возвращает количество столбцов в ссылке. |
|  FORMULATEXT | Returns the formula at the given reference as text | Ф.ТЕКСТ | Возвращает формулу в заданной ссылке в виде текста. |
|  CHOOSE | Chooses a value from a list of values | ВЫБОР | Выбирает значение из списка значений. |
|  XOR | Returns a logical exclusive OR of all arguments | ИСКЛИЛИ | Возвращает логическое исключающее ИЛИ всех аргументов. |
|  ADDRESS | Returns a reference as text to a single cell in a worksheet | АДРЕС | Возвращает ссылку на отдельную ячейку листа в виде текста. |
|  AREAS | Returns the number of areas in a reference | ОБЛАСТИ | Возвращает количество областей в ссылке. |
|  ISNUMBER | Returns TRUE if the value is a number | ЕЧИСЛО | Возвращает значение ИСТИНА, если аргумент ссылается на число. |
|  ISODD | Returns TRUE if the number is odd | ЕНЕЧЁТ | Возвращает значение ИСТИНА, если значение аргумента является нечетным числом. |
|  ISREF | Returns TRUE if the value is a reference | ЕССЫЛКА | Возвращает значение ИСТИНА, если значение аргумента является ссылкой. |
|  ISNONTEXT | Returns TRUE if the value is not text | ЕНЕТЕКСТ | Возвращает значение ИСТИНА, если значение аргумента не является текстом. |
|  ISFORMULA | Returns TRUE if there is a reference to a cell that contains a formula | ЕФОРМУЛА | Возвращает значение ИСТИНА, если имеется ссылка на ячейку с формулой. |
|  ISLOGICAL | Returns TRUE if the value is a logical value | ЕЛОГИЧ | Возвращает значение ИСТИНА, если аргумент ссылается на логическое значение. |
|  ISNA | Returns TRUE if the value is the #N/A error value | ЕНД | Возвращает значение ИСТИНА, если аргумент ссылается на значение ошибки #Н/Д. |
|  ISTEXT | Returns TRUE if the value is text | ЕТЕКСТ | Возвращает значение ИСТИНА, если значение аргумента является текстом. |
|  TYPE | Returns a number indicating the data type of a value | ТИП | Возвращает число, обозначающее тип данных значения. |
|  AND | Returns TRUE if all of its arguments are TRUE | И | Возвращает значение ИСТИНА, если все аргументы имеют значение ИСТИНА. |
|   | Returns the logical value FALSE | ЛОЖЬ | Возвращает логическое значение ЛОЖЬ. |
|  SHEETS | Returns the number of sheets in a reference | ЛИСТЫ | Возвращает количество листов в ссылке. |
|  N | Returns a value converted to a number | Ч | Возвращает значение, преобразованное в число. |
|  NA | Returns the error value #N/A | НД | Возвращает значение ошибки #Н/Д. |
|  SHEET | Returns the sheet number of the referenced sheet | ЛИСТ | Возвращает номер листа, на который имеется ссылка. |
|  IMSIN | Returns the sine of a complex number | МНИМ.SIN | Возвращает синус комплексного числа. |
|  IMSINH | Returns the hyperbolic sine of a complex number | МНИМ.SINH | Возвращает гиперболический синус комплексного числа. |
|  IMSQRT | Returns the square root of a complex number | МНИМ.КОРЕНЬ | Возвращает значение квадратного корня из комплексного числа. |
|  IMSECH | Returns the hyperbolic secant of a complex number | МНИМ.SECH | Возвращает гиперболический секанс комплексного числа. |
|  IMPRODUCT | Returns the product of from 2 to 255 complex numbers | МНИМ.ПРОИЗВЕД | Возвращает произведение от 2 до 255 комплексных чисел. |
|  IMREAL | Returns the real coefficient of a complex number | МНИМ.ВЕЩ | Возвращает коэффициент при вещественной (действительной) части комплексного числа. |
|  IMSEC | Returns the secant of a complex number | МНИМ.SEC | Возвращает секанс комплексного числа. |
|  IMSUB | Returns the difference between two complex numbers | МНИМ.РАЗН | Возвращает разность двух комплексных чисел. |
|  OCT2HEX | Converts an octal number to hexadecimal | ВОСЬМ.В.ШЕСТН | Преобразует восьмеричное число в шестнадцатеричное. |
|  ACCRINT | Returns the accrued interest for a security that pays periodic interest | НАКОПДОХОД | Возвращает накопленный процент по ценным бумагам с периодической выплатой процентов. |
|  ACCRINTM | Returns the accrued interest for a security that pays interest at maturity | НАКОПДОХОДПОГАШ | Возвращает накопленный процент по ценным бумагам, процент по которым выплачивается в срок погашения. |
|  OCT2DEC | Converts an octal number to decimal | ВОСЬМ.В.ДЕС | Преобразует восьмеричное число в десятичное. |
|  IMSUM | Returns the sum of complex numbers | МНИМ.СУММ | Возвращает сумму комплексных чисел. |
|  IMTAN | Returns the tangent of a complex number | МНИМ.TAN | Возвращает тангенс комплексного числа. |
|  OCT2BIN | Converts an octal number to binary | ВОСЬМ.В.ДВ | Преобразует восьмеричное число в двоичное. |
|  IMCOS | Returns the cosine of a complex number | МНИМ.COS | Возвращает косинус комплексного числа. |
|  IMCOSH | Returns the hyperbolic cosine of a complex number | МНИМ.COSH | Возвращает гиперболический косинус комплексного числа. |
|  IMCOT | Returns the cotangent of a complex number | МНИМ.COT | Возвращает котангенс комплексного числа. |
|  IMCONJUGATE | Returns the complex conjugate of a complex number | МНИМ.СОПРЯЖ | Возвращает комплексно-сопряженное комплексного числа. |
|  IMABS | Returns the absolute value (modulus) of a complex number | МНИМ.ABS | Возвращает абсолютную величину (модуль) комплексного числа. |
|  IMAGINARY | Returns the imaginary coefficient of a complex number | МНИМ.ЧАСТЬ | Возвращает коэффициент при мнимой части комплексного числа. |
|  IMARGUMENT | Returns the argument theta, an angle expressed in radians | МНИМ.АРГУМЕНТ | Возвращает значение аргумента комплексного числа (тета) — угол, выраженный в радианах. |
|  IMCSC | Returns the cosecant of a complex number | МНИМ.CSC | Возвращает косеканс комплексного числа. |
|  IMLOG10 | Returns the base-10 logarithm of a complex number | МНИМ.LOG10 | Возвращает десятичный логарифм комплексного числа. |
|  IMLOG2 | Returns the base-2 logarithm of a complex number | МНИМ.LOG2 | Возвращает двоичный логарифм комплексного числа. |
|  IMPOWER | Returns a complex number raised to an integer power | МНИМ.СТЕПЕНЬ | Возвращает комплексное число, возведенное в степень с целочисленным показателем. |
|  IMLN | Returns the natural logarithm of a complex number | МНИМ.LN | Возвращает натуральный логарифм комплексного числа. |
|  IMCSCH | Returns the hyperbolic cosecant of a complex number | МНИМ.CSCH | Возвращает гиперболический косеканс комплексного числа. |
|  IMDIV | Returns the quotient of two complex numbers | МНИМ.ДЕЛ | Возвращает частное от деления двух комплексных чисел. |
|  IMEXP | Returns the exponential of a complex number | МНИМ.EXP | Возвращает экспоненту комплексного числа. |
|  INTRATE | Returns the interest rate for a fully invested security | ИНОРМА | Возвращает процентную ставку для полностью инвестированных ценных бумаг. |
|  IPMT | Returns the interest payment for an investment for a given period | ПРПЛТ | Возвращает проценты по вкладу за данный период. |
|  IRR | Returns the internal rate of return for a series of cash flows | ВСД | Возвращает внутреннюю ставку доходности для ряда потоков денежных средств. |
|  FVSCHEDULE | Returns the future value of an initial principal after applying a series of compound interest rates | БЗРАСПИС | Возвращает будущее значение первоначальной основной суммы после применения ряда (плана) ставок сложных процентов. |
|  DURATION | Returns the annual duration of a security with periodic interest payments | ДЛИТ | Возвращает продолжительность Маколея для ценных бумаг, по которым выплачивается периодический процент. |
|  EFFECT | Returns the effective annual interest rate | ЭФФЕКТ | Возвращает фактическую (эффективную) годовую процентную ставку. |
|  FV | Returns the future value of an investment | БС | Возвращает будущую стоимость инвестиции. |
|  ISPMT | Calculates the interest paid during a specific period of an investment | ПРОЦПЛАТ | Вычисляет выплаты за указанный период инвестиции. |
|  NPV | Returns the net present value of an investment based on a series of periodic cash flows and a discount rate | ЧПС | Возвращает чистую приведенную стоимость инвестиции, основанной на серии периодических денежных потоков и ставке дисконтирования. |
|  ODDFPRICE | Returns the price per $100 face value of a security with an odd first period | ЦЕНАПЕРВНЕРЕГ | Возвращает цену за 100 рублей номинальной стоимости ценных бумаг с нерегулярным (коротким или длинным) первым периодом купона. |
|  ODDFYIELD | Returns the yield of a security with an odd first period | ДОХОДПЕРВНЕРЕГ | Возвращает доход по ценным бумагам с нерегулярным (коротким или длинным) первым периодом купона. |
|  NPER | Returns the number of periods for an investment | КПЕР | Возвращает общее количество периодов выплаты для инвестиции. |
|  MDURATION | Returns the Macauley modified duration for a security with an assumed par value of $100 | МДЛИТ | Возвращает модифицированную продолжительность Маколея для ценных бумаг с предполагаемой номинальной стоимостью 100 рублей. |
|  MIRR | Returns the internal rate of return where positive and negative cash flows are financed at different rates | МВСД | Возвращает внутреннюю ставку доходности, при которой положительные и отрицательные денежные потоки имеют разные значения ставки. |
|  NOMINAL | Returns the annual nominal interest rate | НОМИНАЛ | Возвращает номинальную годовую процентную ставку. |
|  COUPDAYSNC | Returns the number of days from the settlement date to the next coupon date | ДНЕЙКУПОНПОСЛЕ | Возвращает количество дней от даты расчета до срока следующего купона. |
|  COUPNCD | Returns the next coupon date after the settlement date | ДАТАКУПОНПОСЛЕ | Возвращает порядковый номер даты следующего купона после даты соглашения. |
|  COUPNUM | Returns the number of coupons payable between the settlement date and maturity date | ЧИСЛКУПОН | Возвращает количество купонов между датой соглашения и сроком вступления в силу. |
|  COUPDAYS | Returns the number of days in the coupon period that contains the settlement date | ДНЕЙКУПОН | Возвращает количество дней в периоде купона, который содержит дату расчета. |
|  AMORDEGRC | Returns the depreciation for each accounting period by using a depreciation coefficient | АМОРУМ | Возвращает величину амортизации для каждого учетного периода, используя коэффициент амортизации. |
|  AMORLINC | Returns the depreciation for each accounting period | АМОРУВ | Возвращает величину амортизации для каждого учетного периода. |
|  COUPDAYBS | Returns the number of days from the beginning of the coupon period to the settlement date | ДНЕЙКУПОНДО | Возвращает количество дней от начала действия купона до даты соглашения. |
|  COUPPCD | Returns the previous coupon date before the settlement date | ДАТАКУПОНДО | Возвращает порядковый номер даты предыдущего купона до даты соглашения. |
|  DISC | Returns the discount rate for a security | СКИДКА | Возвращает ставку дисконтирования для ценных бумаг. |
|  DOLLARDE | Converts a dollar price, expressed as a fraction, into a dollar price, expressed as a decimal number | РУБЛЬ.ДЕС | Преобразует цену в рублях, выраженную в виде дроби, в цену в рублях, выраженную десятичным числом. |
|  DOLLARFR | Converts a dollar price, expressed as a decimal number, into a dollar price, expressed as a fraction | РУБЛЬ.ДРОБЬ | Преобразует цену в рублях, выраженную десятичным числом, в цену в рублях, выраженную в виде дроби. |
|  DDB | Returns the depreciation of an asset for a specified period by using the double-declining balance method or some other method that you specify | ДДОБ | Возвращает величину амортизации актива за данный период, используя метод двойного уменьшения остатка или иной явно указанный метод. |
|  CUMIPMT | Returns the cumulative interest paid between two periods | ОБЩПЛАТ | Возвращает кумулятивную (нарастающим итогом) величину процентов, выплачиваемых по займу в промежутке между двумя периодами выплат. |
|  CUMPRINC | Returns the cumulative principal paid on a loan between two periods | ОБЩДОХОД | Возвращает кумулятивную (нарастающим итогом) сумму, выплачиваемую в погашение основной суммы займа в промежутке между двумя периодами. |
|  DB | Returns the depreciation of an asset for a specified period by using the fixed-declining balance method | ФУО | Возвращает величину амортизации актива для заданного периода, рассчитанную методом фиксированного уменьшения остатка. |